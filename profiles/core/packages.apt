# This file contains a list of packages to install with apt
# Each line should contain a single package name
# Lines starting with '#' and blank lines will be ignored
# (Don't use trailing comments)

# This is just to have a reasonably functional system, not to have everything you'd want

# For installing other stuff
curl
stow
git

# The good zsh experience
zsh
fzf
ripgrep

# Needed for some vim stuff
luarocks
sqlite3
