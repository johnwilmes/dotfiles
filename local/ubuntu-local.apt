# This file contains a list of packages to install with apt
# Each line should contain a single package name
# Lines starting with '#' and blank lines will be ignored
# (Don't use trailing comments)

build-essential
python-is-python3
wezterm

# Vanilla gnome (select on login screen)
#gnome-session

# To map caps lock as desired (X11)
#xcape

# For extension to disable activities overview on login
#chrome-gnome-shell
#gnome-shell-extension-prefs

